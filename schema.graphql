type Restaker @entity(immutable: false) {
  id: String!
  operator: Bytes!
  timestamp: BigInt!
  strategy: Bytes     # strategy used for deposit
  shares: BigInt      # points/shares deposited
  txHash: Bytes       # optional for verification
}